spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_support_portal
    username: testuser
    password: testpassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
